# Tasks

- [x] 1.1 Write the rules as a capability spec
- [x] 1.2 `test/rules.test.js` — enforce them across every container and recipe,
      written so an object added later is checked without anybody remembering
- [x] 1.3 Check the rules against what is already built, and fix what fails
- [x] 1.4 `npm test`, bump both versions, commit, merge, push

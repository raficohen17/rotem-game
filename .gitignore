.DS_Store
node_modules/
*.log

# Raw drawing scans before background removal — keep the processed PNGs only
assets/drawings/_raw/

# Asset tooling only. The game itself ships no dependencies.
tools/.venv/

# Throwaway harnesses for looking at the art in a browser. One of these was
# swept into a commit by a broad `git add` and shipped.
__preview*.html
